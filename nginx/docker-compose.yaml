version: '3.8'
services:
  nginx-proxy:
    image: nginx:alpine
    container_name: nginx-proxy
    ports:
      - "8080:80"
    volumes:
      - ~/nginx-config/nginx.conf:/etc/nginx/nginx.conf
    command: [nginx, '-g', 'daemon off;']